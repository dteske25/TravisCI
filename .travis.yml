language: csharp
mono: none
dotnet: 5.0.100
solution: TravisCI.sln
branches:
  only:
  - master
install:
  - nuget restore TravisCI.sln
script:
  - dotnet restore
  - msbuild /p:Configuration=Release TravisCI.sln
